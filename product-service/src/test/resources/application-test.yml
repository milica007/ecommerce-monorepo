spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/pms_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  liquibase:
    enabled: true
    drop-first: true

logging:
  level:
    root: INFO
    org.springframework: DEBUG
